# Fake News Classifier (NumPy-only Neural Network)

This project implements a fake news classifier using:
- custom tokenization
- unigram, bigram, trigram vectorizer
- manual neural network (NumPy only)
- Adam optimizer

## Run main
python main.py


